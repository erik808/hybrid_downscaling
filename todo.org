* Implement lookback and a recurrent cell

** DONE Change tensor dimensions inside the model from 4D to 5D
   Dimension should be:
   sample x lookback x lats x lons x channels
   - [X] Achieve this using generators
   - [X] Start by replacing current setup with generators and test
     sanity and performance

** TODO RNN
   Can be
   - [X] SimpleRNN
   - [ ] ESN
   - [X] ConvLSTM
   - [ ] GRU

** DONE Create multiple outputs
   Such that the loss functions will compare several different types
   of predictions to several outputs. This can be used to separate the
   models I think.

*** DONE Factorize the decoder
    - [X] Dynamically choose number of blocks in decoder based on
      number of downsamplings. No, just use same variable used in Encoder.

*** DONE Create autoencoder-only pathway
    Should be doable

*** TODO Create RNN-only pathway
    - [ ] Test passing encoding_layers to DataGenerator
    - [ ] Try to create custom loss inside ae_model

*** DONE Create feedthrough-only pathway
    Already exists and defeats the purpose?

** TODO take another look at the scaling
   Scaling the feedthrough fields with the same scaler as the HR
   fields still gives problems at the boundaries.

** TODO Test different feedthrough
   let FT go through full AE, then combine with decoded RNN output

** TODO Compare against old loss formulation using simple setup

* Housekeeping

** DONE data_manager should become a class
   This will make a lot of things more efficient, especially storage
   of interpolators.

** TODO rename main tuning script
   from autoencoder_original to something that makes sense

** DONE get rid of residual mode in everything
   I don't think it will work. And at a later stage it might be easier
   to implement again if the code is cleaner.

** DONE get rid of timeindices in ae_model
   not necessary if we can use a lookback

** TODO get rid of old code
   put it somewhere for reference

** TODO implement tests
   - [ ] single epoch, short history
